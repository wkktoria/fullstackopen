{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part02/phonebook && npm run build && cp -r dist ../../part03/phonebook-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "wkktoria",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
